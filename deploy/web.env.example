# ============================================================================
# MSS Claw · 前端环境变量
# ----------------------------------------------------------------------------
# 用法：按环境复制成对应文件，再执行构建。
#
#   本地开发：cp deploy/web.env.example apps/web/.env.local
#   测试构建：cp deploy/web.env.example apps/web/.env.test        → npm run build:web:test
#   生产构建：cp deploy/web.env.example apps/web/.env.production  → npm run build:web:prod
#
# 注意：前端【不需要】配置任何 OAuth 参数。
# 登录模式、IDaaS 地址、client_id 全部由后端下发（GET /api/v1/auth/config），
# 所以测试和生产可以共用同一份构建产物，改后端配置即可切换，前端不用重新打包。
# ============================================================================

# 【选填】API 根地址。留空 = 同源 /api（配合 Nginx 反代，内网生产推荐留空）。
# 只有前后端分开部署在不同域名时才需要填。
# VITE_API_BASE_URL=

# 【选填】与后端 API_KEY 对应的客户端密钥；后端没开 API_KEY 就不用填。
# VITE_API_KEY=

# 【调试用，正常部署不要填】强制登录模式，绕过后端下发。
# 只在「想用生产构建在本地调账号密码登录」这种场景下临时用。
# 可选值：password | oauth
# VITE_AUTH_MODE=

# 【选填】纯浏览器演示模式，不连后端。内网部署不要开。
# VITE_FORCE_LOCAL_DEMO=false
